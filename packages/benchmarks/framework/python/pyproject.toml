[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tokagentos-benchmark-framework"
version = "1.0.0"
description = "Tokagent Framework Benchmark — Python Runtime"
requires-python = ">=3.11"
dependencies = [
    "tokagentos>=2.0.0",
    "psutil>=6.0.0",
]

[project.scripts]
bench = "src.bench:main_sync"

[tool.hatch.build.targets.wheel]
packages = ["src"]
