# Wiederherstellung der ACT-Konfiguration

Um das OpenCode-Setup und die Plugins wieder zu aktivieren, verschiebe einfach alle Dateien aus diesem Ordner zurück in das Root-Verzeichnis:

```bash
mv .archived/opencode_setup/* .
mv .archived/opencode_setup/.* .
```

Danach ist OpenCode wieder mit allen Berechtigungen und Plugins konfiguriert.
