/**
 * Capacitor / web shell side effects: design-system CSS and native Capacitor
 * plugin registration. Import once from the host entry (e.g. apps/app main).
 */
import "./styles/styles.css";
import "./styles/brand-gold.css";
import "./platform/native-plugin-entrypoints";
