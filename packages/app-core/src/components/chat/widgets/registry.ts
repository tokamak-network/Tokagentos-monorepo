/**
 * Legacy widget registry — re-exports from the new widget system.
 *
 * Kept for backward compatibility with existing tests and any code
 * that imports `resolveChatSidebarWidgets` from this path.
 */
export { resolveChatSidebarWidgets } from "../../../widgets/registry";
