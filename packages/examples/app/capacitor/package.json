{
  "name": "@elizaos/example-app-capacitor",
  "version": "2.0.0-alpha",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:backend": "bun --cwd backend dev",
    "dev:frontend": "bun --cwd frontend dev",
    "build:frontend": "bun --cwd frontend build",
    "cap:sync": "bunx cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.9.3"
  }
}

