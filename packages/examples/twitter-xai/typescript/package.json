{
  "name": "@elizaos/example-xai-x",
  "version": "2.0.0-alpha",
  "description": "X (Twitter) agent using Grok (xAI) + X API via elizaOS",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run agent.ts",
    "dev": "bun --watch run agent.ts",
    "typecheck": "tsc --noEmit",
    "lint": "bunx @biomejs/biome check --write --unsafe .",
    "lint:check": "bunx @biomejs/biome check ."
  },
  "dependencies": {
    "@elizaos/core": "alpha",
    "@elizaos/plugin-sql": "alpha",
    "@elizaos/plugin-xai": "alpha",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
