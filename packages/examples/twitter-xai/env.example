# =============================================================================
# X Agent Example (Grok + X API)
# =============================================================================
# Copy this file to .env and fill in your credentials.
#
# SAFETY: Start with X_DRY_RUN=true so nothing is posted.
#

# =============================================================================
# Grok (xAI) API (required)
# =============================================================================
XAI_API_KEY=your-xai-api-key
# XAI_BASE_URL=https://api.x.ai/v1
# XAI_SMALL_MODEL=grok-3-mini
# XAI_MODEL=grok-3
# XAI_EMBEDDING_MODEL=grok-embedding

# =============================================================================
# X (formerly Twitter) API v2 authentication (required)
# =============================================================================
# This example defaults to OAuth 1.0a user context:
X_AUTH_MODE=env
X_API_KEY=your-x-api-key
X_API_SECRET=your-x-api-secret
X_ACCESS_TOKEN=your-x-access-token
X_ACCESS_TOKEN_SECRET=your-x-access-token-secret

# Optional (not used by all language implementations):
# X_BEARER_TOKEN=your-app-only-bearer-token

# =============================================================================
# Agent Behavior
# =============================================================================
X_DRY_RUN=true

# Reply to mentions (polls @you and replies)
X_ENABLE_REPLIES=true

# Autonomous posting loop
X_ENABLE_POST=false
X_POST_INTERVAL_MIN=90
X_POST_INTERVAL_MAX=180
X_POST_IMMEDIATELY=false

# Timeline actions (likes/reposts/replies chosen by the agent)
X_ENABLE_ACTIONS=false
X_TIMELINE_MODE=foryou

# Topic discovery + follow/engage
X_ENABLE_DISCOVERY=false
X_MIN_FOLLOWER_COUNT=100
X_MAX_FOLLOWS_PER_CYCLE=5

# Polling cadence (minutes)
X_ENGAGEMENT_INTERVAL_MIN=20
X_ENGAGEMENT_INTERVAL_MAX=40
X_DISCOVERY_INTERVAL_MIN=15
X_DISCOVERY_INTERVAL_MAX=30

# Per-cycle limits
X_MAX_ENGAGEMENTS_PER_RUN=5

# Optional targeting: comma-separated handles or "*" for broad engagement.
# Examples:
# X_TARGET_USERS=@someuser,@anotheruser
# X_TARGET_USERS=*

# =============================================================================
# Database (optional)
# =============================================================================
# For production, use PostgreSQL:
# POSTGRES_URL=postgresql://user:password@localhost:5432/elizaos
#
# For development, PGLite is typically used by default (no config needed).
# PGLITE_DATA_DIR=./data/pglite

