# elizaOS Python packages (installed from local source)
# Install from repo root with:
#   pip install -e packages/python -e plugins/plugin-openai/python -e plugins/plugin-inmemorydb/python

# These are editable installs - paths are relative to repo root
-e ../../packages/python
-e ../../plugins/plugin-openai/python
-e ../../plugins/plugin-inmemorydb/python

# Additional dependencies
python-dotenv
