# Workspace-level env overrides for __APP_NAME__.
#
# Most app-specific frontend flags live under `apps/app/.env.example`.
# Runtime and provider env vars follow the upstream elizaOS conventions.

# ELIZA_API_PORT=31337
# ELIZA_PORT=2138
