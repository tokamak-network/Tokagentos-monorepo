// Stub module used as a Vite alias target for packages with broken exports
// (e.g. @elizaos/skills). The real code handles the missing module gracefully
// via try/catch, so this just needs to exist and be importable.
export default {};
