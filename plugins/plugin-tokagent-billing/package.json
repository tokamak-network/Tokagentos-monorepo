{
  "name": "@tokagent/plugin-tokagent-billing",
  "version": "2.0.0",
  "description": "elizaOS plugin: thin-client billing routes that forward to the hosted tokagent gateway (v2.0.0 — was a per-machine DB-backed gate in v1.x).",
  "type": "module",
  "publishConfig": { "access": "public" },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch",
    "lint": "bunx @biomejs/biome check --write .",
    "lint:check": "bunx @biomejs/biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "bun run build"
  },
  "peerDependencies": {
    "@tokagentos/core": "workspace:*"
  },
  "dependencies": {
    "@tokagentos/billing": "workspace:*",
    "jose": "^5.9.6",
    "viem": "^2.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@tokagentos/core": "workspace:*",
    "@types/bun": "^1.3.5",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  }
}
